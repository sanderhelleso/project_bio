import React from 'react';
import styled from 'styled-components';

const PromoImage = () => {
	return <StyledImage>img</StyledImage>;
};

export default PromoImage;

const StyledImage = styled.div``;
